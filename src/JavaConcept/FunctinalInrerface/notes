A Functional Interface in Java:
Has exactly one abstract method.
Can have default and static methods.
Can be used with lambda expressions or method references.
Annotated with @FunctionalInterface



Built-in functional interfaces:
Function<T,R> → takes T, returns R
BiFunction<T, U, R> -> Takes two inputs (T and U) and returns one output (R).
Consumer<T> → takes T, returns void
Supplier<T> → returns T
Predicate<T> → returns boolean
UnaryOperator<T> → takes and returns same type
BinaryOperator<T> → takes two arguments of same type and returns same type


Combine two Functions using andThen and compose
andThen	-->first function, then second function
compose	 --> second function first, then first function


Predicate<T> has built-in default methods:
and(Predicate other) → returns true if both predicates are true
or(Predicate other) → returns true if either predicate is true
negate() → returns true if predicate is false